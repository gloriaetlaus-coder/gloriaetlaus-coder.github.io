---
name: design-iron-rules
description: 用户设计铁律与语料写作规范。当用户进行任何UI设计、前端开发、文案编写时触发。强制执行：不改用户没说过的东西、禁止"不是A而是B"句式、禁止强行绑定、英文主题词纯直译。
name_cn: 设计铁律
description_cn: 用户设计铁律与语料写作规范，适用于所有tool/APP设计与文案编写场景
create_source: super-agent-skill-creator
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'de9c1105-a2c8-4517-8d5d-55da700eafe9'
  PropagateID: 'de9c1105-a2c8-4517-8d5d-55da700eafe9'
  ReservedCode1: '25e697fc-917f-4ab3-915d-e977c7155ef0'
  ReservedCode2: '25e697fc-917f-4ab3-915d-e977c7155ef0'
---

# 设计铁律与语料写作规范

## 设计铁律（最高优先级）

以下规则适用于所有 tool / APP 设计，不可违反：

1. **禁止修改用户没有说过的东西**：用户说改哪里就改哪里，不擅自乱联想、不自作主张扩展
2. **「不要动」= 完全保持原样**：任何位置、颜色、字体、动画都不改
3. **交付前必须仔细检查**：没改好的继续改；改错了改回用户认可的版本
4. **不做"改进"建议**：对用户已明确的设计（如 slogan 样式、卡片布局、空白风格），不做任何"改进"建议，除非用户主动问
5. **「删除」= 真删掉**：用户说"删掉"就是彻底删除，绝对不要用新字段/新内容去替换或填充，不要留空占位。用户说过："把那些东西全部删掉，我不需要那个东西""为什么一定要用新的字段替换它？卡片上都没有这个东西，那不就代表我不需要这个东西存在吗"——用户要求删除的东西，就是用户不需要的东西，直接删干净
6. **卡片上不显示的字段 = 用户不需要的字段**：如果某个数据字段在 UI 上根本不展示（如历史上的 grade 上签/上上签），它就没有存在意义，不要维护、不要保留、不要扩展
7. **永远不要使用「今朝·Good day」以外的旧项目名**：本项目曾用过的旧英文名/旧中文名（历史遗留，现已废弃）一律禁止再使用，包括文件路径、交付说明、代码注释、语料文档、对话中都不允许出现旧名；用户极度反感旧名字，涉及本项目一律用「今朝·Good day」；本项目文件夹统一用 `good-day`

## 视觉全局铁律（最高优先级）

1. **【颜色锁定】**：项目所有 UI 元素的默认颜色，必须严格保持"淡紫色系"和"银白色系"。绝对禁止突然使用突兀的蓝色、红色、绿色或其他未经用户明确要求的颜色。如果用户说"保持现有颜色"，原颜色绝对不能动
2. **【属性冻结原则（针对"替换字体/颜色"）】**：当用户下达"替换某个元素"的指令时（例如："换个字体"、"换个颜色"），这只代表用户要求修改该元素的单一属性（如 `font-family`）。除了用户明确点名的属性之外，该元素所有的其他属性（包括但不限于：`font-size` 字号、`font-weight` 字粗细、`transform` 倾斜角度、`text-shadow` 阴影、`opacity` 透明度、`padding/margin` 间距、`position` 位置）全部视为"冻结"状态。绝对禁止为了"让新字体更好看"而擅自调整或重置其他属性。如果因为技术原因（例如新字体加载后必须调整字号才能排得下），必须提前告知用户，并得到用户允许后才能调整，绝对不准先斩后奏
3. **【默认保留】**：如果用户没有明确说"斜体要改"，斜体必须原样保留；如果用户没有明确说"要把文字变小"，字号必须原样保留；如果用户没有明确说"去掉特效"，所有动效和光效必须原样保留

## 视觉对称与呼吸感铁律（最高优先级）

1. **【间距对称】**：用户对"间距对称"有极高的、近乎苛刻的要求。垂直方向的间距必须视觉绝对等距，用统一 CSS 参数（如 margin/padding）控制，不允许出现"上下不等"、"左右不齐"的情况
2. **【等距原则】**：当存在多个视觉层级（如 slogan 区→卡片区），相邻层级之间的间距必须视觉等距。具体来说：顶部到第一个元素、元素之间、最后一个元素到下一个区域，这三段间距必须视觉上完全相等，用统一的 CSS 参数控制
3. **【规避裁切原则】**：绝对禁止边缘裁切/截断。文字、图片、卡片等内容不允许被容器边缘切断。出现裁切时，优先通过增加 padding 或允许词组换行来解决，绝不通过缩小字号来解决
4. **【换行尊严原则】**：文字换行时，绝对禁止单个字/单个字母孤立占一行。中文最少两个字才能换行，英文最少两个音节。通过 CSS `word-break: keep-all` + `overflow-wrap: break-word` 或 JS 分段逻辑控制
5. **【排错逻辑顺序】**：出现裁切或布局问题时，按以下顺序排查修复：①压缩 margin/padding → ②允许词组换行（word-break/overflow-wrap）→ ③微调字号（必须先告知用户并获得同意）→ 绝对禁止改 line-height
6. **【多终端适配】**：PC 和手机端都必须保持对称等距不裁切。当两端出现不同的裁切/间距问题时，以手机端为准进行调优（手机端屏幕窄、更容易出问题），PC 端通过 `@media (min-width: 600px)` 覆盖

## 语料写作铁律

以下规则适用于所有文案、语料、释义撰写：

1. **禁止「不是A而是B」句式**——用户极度反感，永久记住。例如"快乐不是终点而是过程"绝对不允许
2. **禁止强行绑定**——例如"悲伤是爱"，有的人悲伤就是悲伤，不要跟爱绑架起来
3. **英文主题词纯直译**——本来什么意思就翻译什么，不加料、不生造、不过度解释
4. **释义围绕"身体/神经/激素"正面作用**，最长3行，末句提供情绪价值
5. **出处两行**（国家·学科，作者），版权意识强倾向公有领域；在 JS 数据中两行用 `||` 连接（如 `'美国·神经科学||沃尔特·坎农（Walter Cannon）'`），第一行国家·学科，第二行作者中英文名或机构中英文名
6. **释义字体**：用系统宋体衬线体（--font-cn：Songti SC 优先），禁引 Google Fonts 的 Noto Serif SC（手机端会渲染成粗黑体），字重 600（700 太粗、400 太细）
7. **source 来源必须补全**：不允许留空（历史上出现过 `'美国·神经科学||'` 空作者的情况），每条出处都要有第二行作者/机构

## 字体授权铁律

1. **只允许自由商用字体**：项目内所有引入的字体，必须采用 SIL Open Font License 1.1 或全平台免费商用授权。严禁使用仅标明"免费下载"但限制商业用途的字体。
2. **引入前必须确认授权**：引入任何新字体前，必须查证其授权条款，确认商用可用后在代码注释中标注授权类型（如 `/* SIL OFL 1.1 */`），否则不得引入

## 本地预览与服务器策略

1. **端口卡顿一次性释放**：如果启动本地服务器的进程卡顿，主动尝试一次性释放并重启端口，不要反复尝试（一次不行就换 `8080` 等备用端口）
2. **Playwright 验证超时即跳过**：如果 Playwright 验证过程极为缓慢（超过 15 秒），直接跳过本地验证，直接生成预览链接交付给用户。不要死磕验证流程，用户的浏览器（Chrome/Safari）最终打开的效果才是标准答案
3. **先清理后启动**：如果环境里存在多次启动服务器未关闭的情况，先清理干净再重新启动
4. **静态文件缓存**：尽量让页面加载流畅，不要每次预览都去重新编译所有数据，如果可能，保存好静态文件的缓存

## 交付前检查清单

每次完成设计/修改后、部署前，必须逐项核对，全部通过才能交付：

1. **只改了用户要求改的地方**：逐一回溯用户本轮提出的每项需求，确认已全部落实；确认没有顺手"改进"用户没提过的位置、颜色、字体、动画
2. **按钮绝对不许乱改**：除非用户明确说"改按钮"，否则按钮的 padding、高度、宽度、flex、min-width、max-width 一律保持原样。用户对按钮高度极度敏感，违反此条是最严重的错误
3. **slogan颜色未被误改**：预览态slogan永远是银白渐变（#FFFFFF→#E8DEF5→#D4C5ED），只有保存下载的图片才用其他颜色
4. **浏览器实测**：用 Playwright 截图（桌面 + 手机端），确认视觉效果符合预期；不只靠代码推理，以实际渲染为准
5. **文字完整性**：所有文字完整显示，无截断、无溢出容器边缘
6. **间距用JS精确测量**：用户要求"大致相等"的间距，用 getBoundingClientRect() 测量实际像素值对比，差距控制在 5px 以内
7. **桌面端和手机端分别验证**：两个断点都要截图+测量，不能只验证一个
8. **字体回退**：Web 字体引入后必须有系统字体回退栈，确保字体加载失败时页面不崩
9. **控制台零报错**：除 favicon 404 外无其他错误
10. **改错了改回去**：如果某项改动后发现改错了，立即恢复到用户认可的版本，不要在错误基础上继续修改
11. **部署后线上验证**：部署完成后必须打开线上地址截图验证，不能只靠本地预览
12. **最终交付前通读skill全文**：交付前必须完整重读本skill文档全部内容，确认没有任何一条被违反，才能交付
13. **保存按钮规则**：手机端保存按钮文字为「长按保存」，操作为长按后保存图片到相册；保存浮层关闭按钮文字为「关闭」（不要写「完成」）；预览态样式与保存图片样式需一致（释义文字浅色细字）
14. **数据替换规则**：替换语料/数据数组时，直接完整替换整个数组，不保留旧字段、不加新字段、不逐条手改

## 用户交互与提交规范

1. **双重视角交叉理解**：用户每次提交修改意见时，可能同时提交两份 Prompt——一份用户自然语言描述（真实意图），一份 AI 辅助生成的结构化修改指令（执行路径）。综合两者才能减少修改次数，一次性改对；用户自然语言的真实意图为首要优先级
2. **保持深度思考过程**：以文字形式展示逻辑推理，帮助用户及时发现理解偏差；即使后续有精准 Prompt 提供，也必须以用户自然语言的真实意图为首要优先级
3. **冒号分隔=并列任务**：用户每条意见用冒号（：）分隔，冒号前面的文本是修改区域，后面是详细修改要求。不同冒号之间的内容属于相互独立的并列任务，逐一修复，不要漏改任何一个冒号后的点
4. **直接改不要长篇预说明**：动手修改后直接生成预览链接；可以解释排查原因，但核心是"直接改"
5. **视觉冲突优先保效果**：如果用户直接指令与代码实现有冲突（如改字体导致按钮变了），优先保证用户视觉上期待的最终效果，选最稳定、最少出 Bug 的方案
6. **全局CSS变量标准**：项目初期建立全局 CSS 变量（CSS Framework），统一定义字号、颜色、字重、间距；修改样式以调整全局变量为主，禁止在单个组件里硬写死数值
7. **满意版打点**：用户明确表示"满意/ok/不错/挺好/很好/可以"时，在代码库中标记该版本为【满意版】
8. **历史考据原则**：某个视觉细节一直改不对时，查阅项目历史记录，寻找用户长期没抱怨且最稳定的"黄金数值"作为默认基准
9. **修改检查点**：动手前先确认理解正确；发现理解偏差时暂停并告知用户

## PC端与移动端样式铁律（最高优先级）

1. **平台差异不是代码错误**：手机与 PC 预览不同，是底层渲染引擎（WebKit vs Chromium）和系统字体加载不同导致。不要强行让两端"一模一样"，而是通过正确 CSS 结构保证两端各自完美
2. **强制媒体查询隔离**：修改"手机端"样式必须写在 `@media (max-width: 768px) { ... }` 里；修改"PC 端"样式必须写在 `@media (min-width: 769px) { ... }` 或默认样式里；绝对禁止在不包裹 `@media` 的情况下直接修改全局字体、颜色、字号和动效
3. **核心原则**：PC 端默认样式是"基底"，手机端是"覆盖"；每次只改用户要求的那一端，绝对不动另一端
4. **终极优化目标**：尽可能使用相对单位（rem、vh、vw）；尽量让 PC 和手机用同一套代码自适应缩放；确实无法自适应时用 `@media` 明确隔离

## 强制交付预览链接规则（最高优先级）

1. **200 OK 即交付**：每次启动本地服务器后，只要命令行提示（如 `GET /index.html 200`）确认网页可以正常访问，立刻停止一切验证动作（包括 curl、截图验证、Playwright 等）
2. **禁止繁琐自检**：绝对禁止在拿到 200 OK 后继续进行繁琐的自检或等待，这会卡住流程，极度影响用户体验
3. **直接生成链接**：获得 200 OK 后，直接生成最终预览链接交付给用户。如果本地端口不是固定的，告诉用户具体的访问地址

## 预览与服务器验证规则

1. **首页 200 = 成功**：启动本地预览时，`/index.html` 返回 200 OK 且能正常渲染，即项目成功运行
2. **忽略 404 接口**：本地环境中 `/api/v1/health` 等非静态接口返回 404 时直接忽略，不因此认为项目运行失败
3. **不强行调用动态接口**：不要在本地环境中调用非静态逻辑接口验证，以 `curl` 抓取首页数据成功为准
4. **超时保护**：不确定是否会卡住时，用 `--max-time` 或 `timeout` 给检查命令设超时（如 5 秒），超时后直接进入下一步
5. **预览成功后不反复刷新**：预览成功后只提供最终链接，无需反复刷新或重启服务器，除非网页源码发生重大逻辑变更

## 卡片主题词排版与防卡边铁律（最高优先级）

1. **【防卡边绝对原则】**：卡片内所有文字元素（主题词、副题、直译、释义、出处）绝对不允许被卡片边缘截断。必须通过充足的 padding + 合理的断行策略确保文字完整显示
2. **【padding 优先】**：出现文字接近边缘时，第一优先增加 padding（两侧至少 12px，中文主题词至少 24px），不要缩小字号
3. **【换行策略】**：中文用 `word-break: keep-all`（不在单字中间断行）；外文用 `white-space: normal` + `overflow-wrap: break-word` + `word-break: normal`（允许按词组换行）
4. **【应急处理顺序】**：卡边问题出现时，按以下顺序处理——①增加 padding → ②允许词组换行 → ③微调字号（≤10%，且必须先告知用户）→ **绝对禁止** `clip`/`overflow:hidden`/`transform:scale` 来强制截断
5. **【移动端专项检查】**：每次调整布局后，必须在 360px 窄屏下检查文字是否溢出。用 `getBoundingClientRect()` 对比元素 right 与卡片 right，差值 > 1px 即为溢出
6. **【长标题特殊处理】**：遇到超长外文句子（如德语长句），确保 `overflow-wrap: break-word` 生效，让句子自然多行换行，不截断单词

## 全面排查与完整性交付铁律（最高优先级）

1. **【未完成持续追踪】**：用户提出多项修改需求时，用 TodoWrite 逐条记录。每完成一项立即标记 completed，未完成的保持 pending 或 in_progress，绝不遗漏
2. **【交付前全面核查】**：每次交付前，回溯用户本轮提出的每一项需求，逐一确认已落实。用清单表格形式列出"已完成/未完成/进行中"，不允许凭记忆判断
3. **【半成品禁止交付】**：如果某项修改只做了一半（如改了 CSS 但没验证、改了代码但没部署、部分元素改了但其他同类元素没改），必须继续完成后再交付，不允许"先交付一部分"
4. **【同类元素一致性】**：如果用户要求"所有主题词都要有某效果"，必须检查所有相关 CSS 类（如 `.sign-poem`、`.sign-poem-cn`、`.sign-cn`、`.sign-literal`、`.sign-cn-en`），确保全部应用，不能只改了其中一个
5. **【CSS 语法完整性】**：每次修改 CSS 后，检查该规则块是否有完整的闭合大括号 `}`、是否缺少 `animation`/`filter` 等关键属性。CSS 语法错误会导致后续规则全部失效
6. **【动效解绑验证】**：当用户要求"两套动效彻底解绑"时，必须确认两套动效使用不同的 keyframes 名称和不同的 animation 属性值，不能共享同一个动画类名

## 卡片排版物理极限与语料库拦截铁律（最高优先级）

1. **【绝对视觉底线】**：整个工具在任何终端（PC/手机）都必须控制在一屏之内，绝对不允许溢出。卡片高度固定，内容超出即为失败
2. **【排版安全阈值】**：字号缩小上限 10%，禁止为容纳长文而猛烈压缩卡片或字号。如果缩小 10% 仍放不下，问题出在语料而非排版
3. **【语料库长度拦截器（强制启用）】**：渲染卡片前必须经过文本长度校验。超过安全阈值的语料自动跳过（不渲染、不抽取），而非强行渲染导致溢出。拦截器阈值标准：
   - 外文主题词（poem）：非 CJK 字符 ≤ 35
   - 中文副题是英文翻译（cn）：ASCII 字符 ≤ 10
   - 释义（meaning）：≤ 45 字符
4. **【hidden 标记机制】**：语料库中每条语料支持 `hidden: true` 字段。标记为 hidden 的条目不参与抽取，但数据保留不删除。用户可通过 `?showall=true` 全量审查模式查看所有条目（含 hidden），决定哪些需要精简后取消隐藏
5. **【长句处理规则】**：优先精简语料本身（缩短释义、精炼翻译），禁止用 `overflow:hidden` 掩盖溢出。精简后达到安全阈值的条目可取消 hidden 标记恢复抽取
6. **【前后端联动检查】**：每次更新语料库后，PC + 手机双端检查所有可抽取语料的渲染效果。新增语料时自动过一遍长度校验，超限直接 hidden
7. **【全量审查模式】**：URL 参数 `?showall=true` 会隐藏正常 UI，以长列表展示所有语料（含 hidden），标注 OK/RISK/HIDDEN 状态，供用户自行审查决定保留或修改

## 任务执行透明度要求

1. **等待状态必须标注**：执行指令时，如果系统需要等待某个子任务完成，或出现网络延迟、模型重试等情况，必须在思考过程或操作栏中明确标明当前状态（如"正在等待网络响应"、"正在尝试连接"、"正在重试"），不要在没有任何提示的情况下长时间静止
2. **切换模型或调整规划先说明**：如果需要切换模型或调整规划，先用一句话告知用户打算，再进行下一步
3. **卡顿超2分钟主动告知**：如果因为外部原因导致卡顿超过2分钟，主动告知用户并提供解决方案

## 卡片排版通用原则

1. **允许自然流式排版**：为了尽量避免出现单个字（或单个字母）独占一行，允许卡片内文本在自动换行时，各行之间不需要强行保持首字垂直对齐。不要求每一行第一个字的首端对齐，这是解决孤字换行问题的核心思路。
2. **严格控制容器空间**：为卡片内的中英文主题词，预留充足的容器宽度。对于较短的中文句子（如少于10个字），必须能保持在一行内完整显示，绝对不允许出现哪怕是几个字的无意义换行。对于长句，允许自然换行。CSS 实现：使用 `text-wrap: balance` 让浏览器自动平衡换行，配合 `word-break: keep-all` 防止中文逐字拆行。
3. **保留绝对红线**：绝对禁止卡边/截断（必须预留合理的内边距）。绝对禁止内容溢出卡片范围。绝对禁止在未明确告知用户的情况下缩小字号来强行塞入内容。
4. **执行顺序**：遇到排版问题时，首要方案是「增加容器宽度」或「允许自然流式排版」，最后才考虑是否调整字号。禁止使用 `overflow: hidden`、`clip`、`transform: scale` 来隐藏溢出内容。禁止使用 `white-space: nowrap` 强制不换行导致溢出（除非该段确实足够短）。
5. **smartSegments 函数规则**：中文短句（≤ maxCn+5 字）整体成段，不拆分，交给 CSS 自然换行。循环切分时，若剩余恰好 1 字，把上一段末尾字挪过来保证本段 ≥ 2 字。纯标点段并入前一段，避免孤立符号占一行。过短段（1-2字中文）并入前段，避免孤立单字占一行。

## 数据层与UI层字体规则完全统一（铁律）

1. **规则定义**：无论是数据层（存储的语料内容）还是UI层（最终渲染出来的预览效果），中英文/外文主题词（poem和cn）绝对不允许出现逗号。
2. **执行标准**：数据层彻底清洗数组里所有的逗号（包括 ASCII 逗号 `,` 和中文逗号 `，`）。UI层确保最终的渲染输出也没有任何逗号。最终结果必须是唯一的：屏幕上的字，和底层存的数据，永远保持100%一致，不允许出现"数据里有但看不到"，或者"数据里没有但显示出来"的偏差。
3. **逗号清除规则**：英文/外文（含拉丁字母）：逗号 → 空格。中文（含 CJK）：中文逗号 `，` 和 ASCII 逗号 `,` → 空字符串直接删除。清除后合并连续多空格为单个空格。
4. **执行要求**：一次性将所有存有逗号的语料在数据源头做彻底清除。把"数据与UI必须保持一致"作为本项目的铁律。不允许在 UI 层用 `replace(/,/g, ...)` 来"掩盖"数据层的逗号——数据源头就不许有。

## 最终交付链路铁律（最高优先级，不可更改）

1. **唯一的链接格式**：当任务完成、需要向用户交付最终结果时，绝对禁止输出任何代码块（例如带有黑底、灰色背景的 ` ```text ` 或 ` ```bash ` 代码框），也绝对禁止输出任何无法直接点击的纯文本地址。唯一的交付格式必须是：可直接在浏览器中点击打开的、高亮显示的超链接（Markdown 标准格式 `[链接文字](https://...)`）。
2. **防止后台部署失败导致的错乱**：如果本次 GitHub 部署失败，导致无法生成新的线上链接，绝对不要将旧的本地链接、本地路径、或者命令行代码（如 `git push`）当成交付物。如果部署失败，请明确告诉用户："部署失败，是因为XXX原因"，并重新尝试部署，直到生成新的、可点击的线上链接为止。绝对禁止出现"昨天能点、今天变成代码块"的倒退现象。
3. **禁止内容泄露**：绝对禁止把任何内部测试代码、Git 命令行、或者无法点击的地址直接暴露给用户。用户只接受最终的、能点开的线上预览链接。
4. **快速检查机制**：在每次交付前，请先自己问自己一句："我给的这东西，用户直接点鼠标能打开吗？" 如果答案是"不能"，则立刻收回，重新生成正确的链接。
5. **交付链接分类**：审核链接 `?review=true` 用于人工审核卡片内容；预览链接用于正式预览抽卡效果。用户审核确认通过后，才能给预览链接。先给审核链接。

## 人工审核功能强制纪律（最高优先级）

1. **【绝对起始点】**：只要用户触发"人工审核"模式，列表必须【永远严格从第1张卡片开始展示】，绝对不能从中间（如第95张）或任何其他位置开始。不允许预设"用户已经看过前面了"的自动跳过逻辑。每次进入审核模式，必须强制清空 localStorage 中的审核记录，让进度从 0 开始。
2. **【全量核验范围】**：审核列表必须展示【所有当前未隐藏的卡片】。用户要求将全部卡片从头到尾检查一遍，以确保项目没有遭到任何未授权的修改。
3. **【禁止自作聪明】**：不允许为了节省步骤、加快进度而自己"跳步"。所有的卡片必须由用户亲自确认后，才能进入下一张。
4. **【触发动作】**：当用户提到"审核"、"检查"、"从头看"等字眼时，必须无条件重置审核队列，从第1张开始重新加载。

## 强制规则：已隐藏卡片彻底封禁（最高优先级）

1. **【隐藏卡片绝对禁止出现】**：所有已经被标记为"隐藏"的卡片，必须严格排除在任何预览数据流之外。无论是预览链接（人工审核状态），还是未来正式上线发布的版本，绝对不允许出现任何一张隐藏卡片的影子。
2. **【强制数据隔离】**：请将所有已隐藏的卡片，从用户端及预览端的抽取池（FORTUNES 数组）和展示列表中，进行物理层面的排除或过滤。这些隐藏卡片只存在于后台的"待处理文件夹"里，不可以被任何页面抓取到。
3. **【编码层面的防泄漏】**：不要只依赖前端的 CSS 隐藏（display: none 或 opacity: 0），因为通过查看源代码依然能嗅探到它们的存在。必须在底层数据返回（API 或前端数组渲染）时，就把已隐藏的卡片提前过滤掉，确保用户只能看到已通过的卡片。
4. **【所有端口一致】**：该过滤规则必须同时生效于：预览链接、人工审核链接、以及最终的正式发布线上链接。

> AI生成