---
name: ui-agent-design-rules
description: 面向 AI Agent 的 UI 设计、前端迭代、内容展示与交付规范。用于减少无关修改、保护已确认设计、避免视觉回归，并提高多任务修改的完整性与可回滚性。
---

# UI Agent 设计与迭代铁律

## 1. 核心原则

1. **只修改用户明确要求修改的内容。** 不因“顺手优化”而扩大修改范围。
2. **单属性修改原则。** 用户只要求替换某一属性时，其他已存在的属性默认冻结。
3. **“不要动”视为冻结指令。** 位置、尺寸、颜色、字体、间距、动画、交互等均保持原样。
4. **已确认的设计是基准。** 用户明确认可的版本、样式或组件状态，后续修改不得无理由改变。
5. **不要擅自提出或执行设计改进。** 只有用户询问优化方案，或当前要求无法实现时，才讨论替代方案。
6. **删除就是删除。** 用户要求删除的内容应真正从对应的数据、UI 或逻辑中移除，不用占位内容或无意义字段替代。
7. **当前异常不自动等于设计规范。** 错位、溢出、缺失、错误换行等状态应先判断是否为问题，再决定是否保留。

## 2. 需求理解

1. 同一轮存在多个任务时，逐项识别并执行，不遗漏、不合并成模糊目标。
2. 自然语言中的具体限制优先于泛化性的“优化”判断。
3. 对用户明确指定的对象、范围和属性建立修改边界。
4. 如果任务涉及多个终端，明确区分 PC 与移动端，不默认互相修改。
5. 不因发现无关问题而顺手修改其他组件；如发现确实阻碍当前任务的问题，再说明原因。

## 3. 视觉修改规则

1. **已确认视觉参数优先保持稳定。** 包括字体、字号、字重、颜色、倾斜、间距、圆角、阴影、发光、动画等。
2. **视觉层级应清晰。** 标题、主题词、正文、辅助信息和操作按钮应保持明确的层级关系。
3. **优先保证完整显示。** 禁止文字被裁切、截断、溢出或贴近容器边缘。
4. **不要把缩小字号作为第一解决方案。** 出现空间不足时，优先检查容器空间、内外边距和自然换行。
5. **避免孤立字符。** 中文尽量避免单字独占一行；英文避免产生不自然的孤立字符或短片段。
6. **保持视觉呼吸感。** 相邻层级的间距应有明确规律，避免无理由的局部挤压或过度留白。
7. **同类组件保持一致。** 修改一个组件后，应检查同类组件是否需要保持同样的结构和规则，但不得因此扩大修改范围。

## 4. 响应式设计

1. PC 与移动端允许存在合理的尺寸和布局差异，不要求像素级完全一致。
2. 响应式修改应尽量使用清晰的断点或组件级规则，避免用大量互相覆盖的临时样式解决问题。
3. 用户只要求修改某一端时，默认保护另一端现有表现。
4. 移动端优先检查：文字完整性、按钮可操作性、容器宽度、滚动位置和边缘安全距离。
5. PC 端优先检查：整体比例、内容密度、居中关系和横向空间利用。

## 5. 数据与 UI 的一致性

1. **数据源是真实来源。** 不应长期依赖 UI 层的字符串替换来掩盖数据问题。
2. 数据清洗规则应尽量在数据进入渲染流程前完成。
3. 如果某类字符、格式或长度被定义为禁用条件，应在数据层建立明确的校验。
4. UI 中隐藏某条数据，不等于可以擅自删除源数据；除非用户明确要求删除。
5. `hidden`、草稿、审核中等状态应有明确的数据流规则。需要隐藏时，应优先在数据或逻辑层过滤，而不是仅靠 CSS 隐藏。
6. 修改数据结构前，应确认现有 UI、筛选、排序、审核和恢复流程是否依赖相关字段。

## 6. 内容与排版

1. 文案应优先准确、自然、易理解，避免为了“高级感”增加没有必要的修辞。
2. 用户已经确定的文案不要擅自改写。
3. 翻译类内容应保持原意，不额外添加原文没有表达的信息。
4. 内容长度应与实际容器能力匹配。发现超长内容时，优先处理内容本身或合理换行，而不是用 CSS 隐藏问题。
5. 涉及心理、健康、科学等主题时，避免绝对化、因果过强或未经依据的承诺。

## 7. 组件与代码修改

1. 修改前先确认现有实现在哪里，以及该实现是否被其他组件复用。
2. 不为了完成一个局部视觉修改而重写整个组件或全局样式。
3. 如果必须修改共享组件或全局规则，应先判断影响范围，再执行。
4. 新增样式应尽量沿用现有组件结构和命名体系，避免制造重复规则。
5. 发现已有机制较复杂时，不应仅因为代码“不够简洁”就擅自重构；先确认它承担的功能。
6. 已验证有效的特殊处理应保留其目的与约束，除非用户明确要求替换。

## 8. 审核与验证

1. 修改完成后，应检查本轮用户提出的每一项要求是否实际落实。
2. 检查重点应包括：文字完整性、位置、尺寸、颜色、响应式表现、交互状态和控制台明显错误。
3. 如果修改的是局部组件，应重点验证该组件以及可能受共享样式影响的同类组件。
4. 验证方式应与任务规模匹配。简单修改不需要为了形式而执行大量重复检查。
5. 如果用户明确要求截图、浏览器实测或视觉对比，应按该要求完成验证。
6. 不把“代码没有报错”直接等同于“视觉任务已经完成”。

## 9. 多任务与交付完整性

1. 将本轮任务拆成可核对的事项，完成一项确认一项。
2. 交付前回看用户原始要求，而不是只检查自己认为重要的部分。
3. 任何只完成一半的任务都不应作为完整结果交付。
4. 如果某项要求无法完成，应明确说明具体阻碍，不用其他结果冒充完成。
5. 同类元素如果存在明确的统一规则，应确认修改没有造成局部与整体不一致。

## 10. 版本保护与回滚

1. **重大修改前建立版本快照。** 推荐使用 Git commit 或其他可靠版本控制。
2. 用户明确认可一个版本后，可将其作为新的稳定基准。
3. 重大修改应能够回到上一个稳定版本，不依赖手工复制文件夹。
4. 回滚应恢复到明确的已知版本，而不是凭记忆重新修改。
5. 不要为了回滚而随意覆盖用户未要求修改的数据或配置。

## 11. 项目知识的保存方式

1. Skill 应记录**跨任务仍然有效的规则和重要项目机制**，而不是记录每一次修改的全过程。
2. 某项机制如果后续任务仍可能依赖，应保留“它做什么”以及“为什么不能随意改变”的简短说明。
3. 已经废弃的规则应删除或明确标记为废弃，避免 Agent 同时遵循新旧规则。
4. 不为每一种组件、每一个像素值都建立独立文档；只有在信息确实复杂、稳定且需要跨任务复用时才拆分文档。
5. 项目代码、数据和当前已确认设计共同构成事实来源；Skill 负责补充代码本身无法表达的设计意图与修改边界。

## 12. 最终检查清单

- [ ] 只修改了用户要求的范围
- [ ] 用户明确要求保留的属性没有被改变
- [ ] 没有把当前问题当成设计规范
- [ ] 文字没有裁切、溢出或产生明显孤字
- [ ] PC 与移动端没有发生无关的连带修改
- [ ] 数据层没有被 UI 层的临时替换逻辑掩盖
- [ ] hidden 等状态没有被擅自删除或解除
- [ ] 本轮所有任务均已完成
- [ ] 已确认版本在必要时可以回滚
- [ ] 交付结果与用户实际要求一致

> **原则总结：稳定已确认的部分，精确修改被要求的部分；保留必要的项目知识，避免无意义的文档和代码扩张。**
