---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '9f96ad83-42cd-4c88-a53f-44ca505ddca1'
  PropagateID: '9f96ad83-42cd-4c88-a53f-44ca505ddca1'
  ReservedCode1: '563e6196-1c8a-4b4d-9331-35c54d62973c'
  ReservedCode2: '563e6196-1c8a-4b4d-9331-35c54d62973c'
---

## 设计铁律（最高优先级，适用于所有 tool / APP 设计）
- 禁止修改用户没有说过的东西：我说什么就是什么，我说改哪里就改哪里，不要擅自乱联想、不要自作主张扩展
- 「不要动」= 保持原样，不修改任何位置、颜色、字体、动画
- 「删除」= 真删掉：用户说删掉就是彻底删除，不要用新字段/新内容替换或填充、不要留空占位、不要问"要不要换成别的"。卡片上不显示的东西 = 用户不需要的东西，直接删干净
- 交付前必须仔细检查：没改好的继续修改；改错了的改回去（恢复用户认可的版本）
- 对用户已明确的设计（如 slogan 样式、卡片布局、空白风格）不做任何"改进"建议，除非用户主动问

## 语料写作铁律
- 禁止「不是A而是B」句式（如"快乐不是终点而是过程"）——用户极度反感，永久记住
- 禁止强行绑定（如"悲伤是爱"——有的人悲伤就是悲伤，不要跟爱绑架起来）
- 英文主题词纯直译：本来什么意思就翻译什么，不加料、不生造、不过度解释
- 释义围绕"身体/神经/激素"正面作用，最长3行，末句提供情绪价值
- 出处两行（国家·学科，作者），JS中两行用 `||` 连接，第二行作者/机构不能留空

## 今朝·Good day 项目规则
- 中英 slogan：当前这版的样子就是最终版，永远不要改动位置/样式/颜色/动画
- 卡片高度 558px（桌面）/ min(78vh,640px)（手机）/ min(80vh,620px)（360px 窄屏），长方形
- 卡片内「中文主题词与释义之间的大片空白」是用户特意要的设计，不是 bug，不要修
- 释义字体：系统宋体（Songti SC 优先），禁用 Google Fonts Noto Serif SC（手机端渲染成粗黑体难看），字重 600
- 保存图片释义文字：颜色浅、字体细（接近预览观感），不要深色粗体
- 手机端保存按钮：文字「长按保存」，操作=长按后保存图片到相册
- 保存浮层关闭按钮文字：「关闭」（不要写「完成」）
- 中文主题词：直译主题词意思，要短促铿锵有力，最好像四字成语，不要扔长句
- 底部按钮：3个等宽（复制/长按保存/首页）或用户指定的数量，grid三列/多列铺满，不许擅自改成窄条
- 语料数据：不保留 grade（上签/上上签）字段，卡片不显示的一律不要；source 两行用 `||` 连接
- 永远不要使用旧项目名（历史遗留的英文/中文旧名）——用户极度反感；本项目统一叫「今朝·Good day」，文件夹统一用 `good-day`

> AI生成